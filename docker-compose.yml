services:
  api:
    build: . 
    container_name: documind 
    ports: 
      - "8000:8000"
    env_file: 
      - .env
    volumes:
      - ./chroma_db:/app/chroma_db
      - ./uploads:/app/uploads
    restart: unless-stopped 